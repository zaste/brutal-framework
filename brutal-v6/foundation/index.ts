/**
 * BRUTAL V6 Foundation
 * The constitution that prevents regression
 */

export { validate } from './validate';
export type { ValidationResult, Violation } from './validate';

// That's it. ONE function. No more public API.